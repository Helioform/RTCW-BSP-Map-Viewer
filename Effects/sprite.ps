
Texture2D shaderTexture;
SamplerState SampleType;



struct PixelInputType
{
    	float4 position : SV_POSITION;
    	float2 tex : TEXCOORD0;
        float4 color : COLOR;
};


float4 SpritePixelShader(PixelInputType input) : SV_TARGET
{
	float4 tcolor, color;	
	tcolor = shaderTexture.Sample(SampleType, input.tex);
/*	
	if(tcolor.r == 0.0f && tcolor.g == 0.0f && tcolor.b == 0.0f)
	{
		color.a = 0.0f;
		color = tcolor*input.color;
	}
	else
	{

		color.a = 1.0f;
		color = tcolor*input.color;
	}	
*/
    	return tcolor;//*input.color;
}
